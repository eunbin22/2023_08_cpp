
#include <cstdio>
#include <cmath> // sin(), cos(), tan(), exp() =>지수함수, log(), pow()=>거듭제곱
#include <stdlib.h> // malloc, free, rand => 의사 난수 생성, 의사 난수 시드 설정
#include <ctime> // time, clock

struct Person {
	int age;
	char name[100];
};

int add(int a, int b);
